#include<iostream>
#include<cmath>
using namespace std;
int sum(int a, int b){
    return a+b;
}

int main(){
    // int a, b;
    // cout<<"Enter number 1: ";
    // cin>>a;
    // cout<<"Enter number 2: ";
    // cin>>b;
    // cout<<sum(a, b);

    int a, b;
    cout<<"Enter a number: ";
    cin>>a;
    cout<<"Enter a power: ";
    cin>>b;
    // cout<<"square root is: "<<sqrt(a);

    // cout<<"cube root is: "<<cbrt(a);

    // cout <<"minimum and maximum of "<<a<<" and "<<b<<" is: "<<min(a,b)<<" and "<<max(a,b);

    // cout<<"power raised to "<<b<<" is: "<<pow(a, b);
}
